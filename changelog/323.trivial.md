Migrated the development tooling from Poetry to [uv](https://docs.astral.sh/uv/) and switched formatting/linting fully to [ruff](https://docs.astral.sh/ruff/) (replacing black and blackdoc). The version-bump workflow now uses `uv version` with a vendored composite action.
