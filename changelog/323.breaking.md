Dropped support for Python 3.10; scmdata now requires Python >=3.11. The minimum supported ``xarray`` was raised to 2025.12 and ``scipy`` to 1.13, which are the versions required to work with pandas 3 and numpy 2 (Python 3.10's last ``xarray`` release predates pandas-3 support).
