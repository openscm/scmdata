Fixed :meth:`ScmRun.from_nc` under pandas >= 2.1 (including pandas 3). The densified netCDF grid produced spurious all-NaN timeseries that pandas no longer drops automatically inside ``stack``, which caused incorrect round-trips and ``Index contains duplicate entries`` errors when an ``_id`` dimension was present.
